import streamlit as st import requests import collections import random import urllib3  # Desativa avisos de requisições HTTPS inseguras devido ao verify=False urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)  # CONFIGURAÇÃO DA PÁGINA st.set_page_config(page_title="IA Lotofácil Pessoal", page_icon="📊", layout="centered") st.title("📊 Analisador de Tendências - Lotofácil") st.write("Dados extraídos em tempo real via API oficial da Caixa.")  # 1. FUNÇÃO PARA BUSCAR OS ÚLTIMOS 200 RESULTADOS @st.cache_data(ttl=3600)  # Guarda os dados por 1 hora para o app ficar rápido def buscar_dados_lotofacil():     try:         # URL oficial corrigida         url_base = "https://servicebus2.caixa.gov.br/portaldeloterias/api/lotofacil"         response = requests.get(url_base, verify=False, timeout=10)         ultimo_concurso = response.json()['numero']          todos_sorteios = []          # Varredura dos últimos 200 concursos         with st.spinner("Analisando o banco de dados dos últimos 200 sorteios..."):             for i in range(ultimo_concurso - 199, ultimo_concurso + 1):                 url = f"{url_base}/{i}"                 res = requests.get(url, verify=False, timeout=5)                 if res.status_code == 200:                     dados = res.json()                     dezenas = sorted([int(x) for x in dados['listaDezenas']])                     todos_sorteios.append(dezenas)         return todos_sorteios, ultimo_concurso     except Exception as e:         st.error(f"Erro ao conectar com o servidor da Caixa: {e}")         return None, None  # EXECUTA A BUSCA AUTOMÁTICA historico_200, ultimo_num = buscar_dados_lotofacil()  if historico_200:     st.success(f"Análise concluída com sucesso! Recorte: Concurso {ultimo_num-199} até o {ultimo_num}.")      # 2. SEPARAÇÃO E PROCESSAMENTO DOS NÚMEROS (IA & Estatística)     todos_numeros_sorteados = [num for sorteio in historico_200 for num in sorteio]     contagem = collections.Counter(todos_numeros_sorteados)      # As 5 dezenas mais sorteadas no período     top_5_com_frequencia = contagem.most_common(5)     as_5_mais = [item[0] for item in top_5_com_frequencia]      # Grupos Estruturais Fixos do Volante (1 a 25)     todos_impares = [n for n in range(1, 26) if n % 2 != 0]     todos_pares = [n for n in range(1, 26) if n % 2 == 0]      # 3. INTERFACE VISUAL DO SEU APP     col1, col2 = st.columns(2)      with col1:         st.markdown("### 🔢 Divisão Fixa do Volante")         st.write(f"**Ímpares (13 dezenas):** \n`{str(todos_impares)[1:-1]}`")         st.write(f"**Pares (12 dezenas):** \n`{str(todos_pares)[1:-1]}`")      with col2:         st.markdown("### 🔥 As 5 Mais Sorteadas")         for idx, (num, freq) in enumerate(top_5_com_frequencia):             st.write(f"**{idx+1}º Lugar:** Dezena `{num:02d}` — Sorteada {freq} vezes")      st.markdown("---")      # 4. MÓDULO INTELIGENTE DE GERAÇÃO DE JOGOS     st.subheader("🎲 Gerador Inteligente de Palpites")     st.write("O sistema usa as 5 mais quentes e equilibra o jogo com 8 ímpares e 7 pares automaticamente.")      qtd_jogos = st.slider("Quantos cartões você quer gerar?", min_value=1, max_value=10, value=3)      if st.button("Gerar Jogos Otimizados"):         st.markdown("### 📝 Seus Cartões Prontos:")          for i in range(qtd_jogos):             # Inicia o jogo contendo as 5 mais sorteadas obrigatoriamente             jogo = list(as_5_mais)              # Conta quantos ímpares e pares já existem dentro das 5 mais selecionadas             impares_fixos = [n for n in jogo if n % 2 != 0]             pares_fixos = [n for n in jogo if n % 2 == 0]              # Meta matemática ideal (8 Ímpares / 7 Pares)             meta_impares = 8             meta_pares = 7              # Calcula quantos faltam para atingir o equilíbrio perfeito             faltam_impares = meta_impares - len(impares_fixos)             faltam_pares = meta_pares - len(pares_fixos)              # Cria lista de opções removendo números que já estão no jogo             opcoes_impares = [n for n in todos_impares if n not in jogo]             opcoes_pares = [n for n in todos_pares if n not in jogo]              # Sorteia o restante obedecendo as restrições estritas de proporção             jogo.extend(random.sample(opcoes_impares, faltam_impares))             jogo.extend(random.sample(opcoes_pares, faltam_pares))              # Organiza em ordem crescente             jogo.sort()              # Formata os números com dois dígitos (ex: 01, 02)             jogo_formatado = " - ".join(f"{num:02d}" for num in jogo)              st.info(f"**Jogo {i+1}:** {jogo_formatado}") else:     st.warning("Não foi possível carregar os dados. Verifique sua conexão com a internet ou tente novamente mais tarde.") 
